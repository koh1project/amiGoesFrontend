export type ConnectFilters = {
  to: string;
  from: string;
  sliderValue: number;
  date: string;
  activities: string[];
  preference: string[];
  preferenceAge: {
    minimumAge: number;
    maximumAge: number;
  };
};

export type LooseObject = {
  [key: string]: any; // Generated by https://quicktype.io
};
export interface Amigo {
  _id: string;
  bio: string;
  birthday: string;
  connectPreferences: ConnectPreferences;
  contact: Contact;
  createdAt: string;
  favorites: string[];
  gender: string;
  hobbies: string[];
  homeCountry: string;
  isVerified: boolean;
  languages: string[];
  name: string;
  updatedAt: string;
}

export interface ConnectPreferences {
  activities: string[];
  currentLocation: CurrentLocation;
  fromDate: string;
  gender: string[];
  isInvisible: boolean;
  maxAge: number;
  minAge: number;
  toDate: string;
}

export interface CurrentLocation {
  latitude: string;
  longitude: string;
}

export interface Contact {
  email: string;
  phoneNumber: string;
}
